import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: [
      "ecoprism.com",
      "white-tillet.com",
      "www.netzeronews.io",
      "minurso.unmissions.org",
      "av.sc.com",
      "fastercapital.com",
    ], // Add external domain here
  },
};

export default nextConfig;
